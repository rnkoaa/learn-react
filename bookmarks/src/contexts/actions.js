
  // remove the bookmark from the server.
  export const deleteBookMark = async id => {
    // const { bookmarks } = this.state;
    // const bookmark = bookmarks.find(b => b.id === id);
    // if (bookmark) {
    //   const deletedBookmark = await bookmarkService.remove(bookmark.id);
    //   if (deletedBookmark) {
    //     bookmarkService.filterBookmark(bookmarks, deletedBookmark);
    //     this.setState({ bookmarks });
    //   }
    // }
  };

  export const updateBookmark = async (id, bookmark) => {
    // const { bookmarks } = this.state;
    // const bookmark = bookmarks.find(b => b.id === id);
    // if (bookmark) {
    //   bookmark.archived = true;
    //   const archivedBookmark = await bookmarkService.update(bookmark.id, bookmark);
    //   if (archivedBookmark) {
    //     bookmarkService.filterBookmark(bookmarks, archivedBookmark);
    //     this.setState({ bookmarks });
    //   }
    // }
  };

  export const createBookmark = async (bookmark) => {
    // const { bookmarks } = this.state;
    // const bookmark = bookmarks.find(b => b.id === id);
    // if (bookmark) {
    //   bookmark.archived = true;
    //   const archivedBookmark = await bookmarkService.update(bookmark.id, bookmark);
    //   if (archivedBookmark) {
    //     bookmarkService.filterBookmark(bookmarks, archivedBookmark);
    //     this.setState({ bookmarks });
    //   }
    // }
  };

//   export const restoreArchive = async id => {
    // const { bookmarks } = this.state;
    // const bookmarkRestore = bookmarks.find(b => b.id === id);
    // if (bookmarkRestore) {
    //   bookmarkRestore.archived = false;
    //   const restoredBookmark = await bookmarkService.update(id, bookmarkRestore);
    //   if (restoredBookmark) {
    //     bookmarkService.filterBookmark(bookmarks, restoredBookmark);
    //     this.setState({ bookmarks });
    //   }
    // }
//   };